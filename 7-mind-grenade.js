const num1= 5;
const num2 = 10;

function addvalues(){
    console.log(`sum is : ${num1 + num2}`);
}
addvalues()

//when any module exports it actulay wrap entire code in functiuon, since here it is already function not exporting it using command